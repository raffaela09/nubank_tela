import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    container: {
        flexDirection: "row",
        padding: 12,
        alignItems: "center",
    },
    rectangle: {
        width: "95%",
        height: 30,
        backgroundColor: "#f5f5f5",
        borderRadius: 5
    }
})