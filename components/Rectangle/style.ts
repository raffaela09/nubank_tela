import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    rectangle: {
        margin: 12,
        width: "93%",
        height: 120,
        backgroundColor: "#2f2f31b4",
        borderRadius: 12,
        alignItems: "flex-end",
        justifyContent: "center",
        padding: 12
    }
})